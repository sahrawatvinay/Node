let city = ["a", "b", "c"];
let cityMap = city.map((ct) => ct + "!");
console.log(cityMap);

let numbers = [1, 2, 3, 4, 5];
let evenNum = numbers.filter((num) => num % 2 === 0);
console.log(evenNum);
